const form = document.getElementById('contactForm'), status = document.getElementById('contactStatus'), agentUrl = window.LEAD_AGENT_URL || 'http://localhost:8787';
form?.addEventListener('submit', async event => { event.preventDefault(); const values = new FormData(form); status.hidden = false; status.textContent = 'Sending your message…'; try { const response = await fetch(`${agentUrl}/api/contact`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ name: values.get('name'), email: values.get('email'), company: values.get('company'), phone: values.get('phone'), details: Object.fromEntries(values.entries()) }) }); if (!response.ok) throw new Error('request failed'); form.reset(); status.textContent = 'Thank you. We received your message and will respond shortly.'; } catch { status.textContent = 'We could not send your message right now. Please email info@sea-and-shore.com or call +31 (0)10 409 01 30.'; } });
